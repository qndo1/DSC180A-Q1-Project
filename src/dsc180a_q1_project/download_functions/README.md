This folder contains code for downloading and preprocessing MoCap recordings.

Run download_mocap.py first to collect raw .txt files.
Then run txt_to_csv.py to convert them into .csv format.

The output will appear in:
src/datasets/txt_files
src/datasets/csv_files